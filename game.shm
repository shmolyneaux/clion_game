struct Point {
    x,
    y,
}

let p = Point(1, 3);

fn loop() {
    if ig_begin("from shimlang") {
        ig_text(p);
        ig_end();
    }

    draw_rect(
        10,
        20,
        200,
        100,
    );
    let foo = [];
}