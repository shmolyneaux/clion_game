{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "name": "Debug Game (LLDB)",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: build"
        }
    ]
}
